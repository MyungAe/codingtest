# Graph
# graph를 그리는 종류 : 인접 리스트(간선 개수 기준, 공간 win) vs 인접 행렬(정점 개수 기준, 시간 win)

# BFS, DFS
# 최단거리 탐색, 끝을 본다 = 가중치O : DFS + stack
# 나머지 = 가중치 X : BFS + queue

# 리스트에서 뒤에 있는 원소를 빼면 DFS, pop()
# 앞에 있는 원소를 빼면 BFS, pop(0)

# 준비물
# 1. 인접 그래프
# 2. 목적지 리스트
# 3. 방문 여부 확인용 리스트

# queue에서 앞으로도, 뒤로도 뺄 수 있는 자료구조 = Double-Ended-Queue, Dequeue
# 정렬 개념 X, 맨 앞 & 맨 뒤만 표시

# heap << 넣자마자 정렬되는 Tree 자료구조. 정렬 시간이 짧다 O(log n)

# 파이썬 비교우위
# 조합, 순열, heap, deque




